echo "1" >> README.md
sudo git add . --all
sudo git commit -m "increment"
sudo git push origin master

echo "1" >> README.md
sudo git add . --all
sudo git commit -m "increment"
sudo git push origin master

echo "1" >> README.md
sudo git add . --all
sudo git commit -m "increment"
sudo git push origin master

echo "1" >> README.md
sudo git add . --all
sudo git commit -m "increment"
sudo git push origin master

echo "1" >> README.md
sudo git add . --all
sudo git commit -m "increment"
sudo git push origin master

echo "1" >> README.md
sudo git add . --all
sudo git commit -m "increment"
sudo git push origin master
